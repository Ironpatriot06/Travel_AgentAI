import streamlit as st
from blog_agent import search_blog

st.title("🧳 Travel Blog Assistant")
query = st.text_input("Ask a travel question based on the blog:")

if query:
    with st.spinner("Searching..."):
        response = search_blog(query)
    st.markdown("### ✨ Answer:")
    st.write(response)

